import { CellType, ExtendedTable, Step } from '../../../types/Types';

export const byBlockLineReduction = (table: ExtendedTable, block: CellType[], queue: Step[], blockIdx: number) => ({
   fn: () => {},
   name: 'byBlockLineReduction'
});